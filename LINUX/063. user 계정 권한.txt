######################
# NATEIFS
######################
#sudo visudo 로 들어가서 password 없이 sudo 할수 있는 명령어 샘플 보고 users 그룹명으로 하나 추가후 저장

# 사용자 추가 및 암호 설정
useradd -m -d /home/nate -g users -s /bin/bash nate
echo "wideps1!" | passwd nate --stdin
useradd -m -d /home/vam -g users -s /bin/bash vam
echo "wideps1!" | passwd vam --stdin
useradd -m -d /home/pmh -g users -s /bin/bash pmh
echo "wideps1!" | passwd pmh --stdin
useradd -m -d /home/tomcat -g users -s /bin/bash tomcat
echo "wideps1!" | passwd tomcat --stdin

passwd nate

adduser username

usermod -aG wheel username

# 그룹에 추가하는 명령어
usermod -aG wheel username 

# 그룹을 바꾸는 명령어
usermod -g users nsuser


# 전체 PATH 보여주기
vi /etc/bashrc
PS1='\u@\h:\w\$ '
